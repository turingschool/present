<% @courses.each do |course| %>
    <%= button_to "#{course[:abbrv]} - #{course[:name]}", new_turing_module_populi_student_path(@module), params: {course_instance_id: course[:instanceid]}, method: :get %>
<% end %>